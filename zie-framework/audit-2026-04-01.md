# Audit Report — 2026-04-01

**Version**: v1.15.0 | **Stack**: Python 3.x, Claude Code plugin
**Domain**: Solo developer SDLC framework plugin
**Agents**: 4 parallel (Security+Deps, Code Health+Perf, Structural+Obs, External Research)
**Findings**: 28 raw → 9 actionable added to backlog | **Deferred**: 19

---

## HIGH — Quick Wins

| Finding | Dimension | Impact | Effort |
|---------|-----------|--------|--------|
| `audit-write-adr-cache-contract` — `write_adr_cache()` returns `bool` but all 4 commands (zie-audit/plan/implement/sprint) document tuple `(True, path)`; ADR context bundles silently get `None` | Structural/Docs | 4 | 1 |
| `audit-hook-outer-guard` — `session-resume.py`, `session-learn.py`, `wip-checkpoint.py` missing outer `try/except`; `atomic_write` re-raises `OSError`; violates CLAUDE.md two-tier contract | Observability | 4 | 2 |
| `audit-pytest-cve-requirements` — CVE-2025-71176 pytest ≤9.0.2 TOCTOU tmpdir priv-esc (public PoC); no `requirements.txt` means CI installs vulnerable version silently | Dependency/Security | 4 | 2 |
| `audit-roadmap-cache-sanitize` — `get_cached_roadmap`/`write_roadmap_cache` skip `session_id` sanitization + hardcode `/tmp/` + `session-cleanup.py` never cleans `zie-{session_id}/` dirs (naming mismatch) | Security/Arch | 3 | 2 |

## MEDIUM — Quick Wins

| Finding | Dimension | Impact | Effort |
|---------|-----------|--------|--------|
| `audit-playwright-version-check` — CVE-2025-59288 Playwright <1.55.1 MitM via `curl -k` installer; no version check on `playwright_enabled: true` startup | Dependency/Security | 3 | 1 |
| `audit-safety-check-regex-precompile` — `safety-check.py` recompiles `BLOCKS`/`WARNS` on every `PreToolUse:Bash`; `intent-sdlc.py` already uses `COMPILED_PATTERNS` at module level | Performance | 3 | 1 |
| `audit-readme-sprint-command` — `/zie-sprint` (v1.15.0) absent from README.md commands table (primary public-facing doc) | Docs | 3 | 1 |
| `audit-ruff-lint-gate` — no Python linter in pre-commit/Makefile/CI; hooks use type annotations; `ruff` is 2025 community standard | DX/Quality | 3 | 2 |

## MEDIUM — Strategic

| Finding | Dimension | Impact | Effort |
|---------|-----------|--------|--------|
| `audit-coverage-gate-raise` — gate frozen at 43% since ADR-027; `sitecustomize.py` measurement now stable; community standard 70–80% for hook-based tooling | Test Coverage | 3 | 3 |

---

## MEDIUM / LOW — Deferred (not added to backlog)

| Finding | Dimension | Severity |
|---------|-----------|----------|
| `knowledge-hash-mtime-gate` — `rglob('*')` twice on every SessionStart, no caching | Performance | MEDIUM |
| `subagent-stop-atomic` — bare `open("a")` in subagent-stop; not atomic under concurrent sprint | Observability | MEDIUM |
| `project-md-docs-sync` — docs-sync-check skill absent from PROJECT.md skills table | Docs | MEDIUM |
| `stopfailure-stderr` — only 2 of many error types surfaced to stderr | Observability | LOW |
| `cache-write-silent-failure` — `write_roadmap_cache`/`write_git_status_cache` swallow exceptions silently | Observability | LOW |
| `ci-matrix` — CI tests only ubuntu-latest + Python 3.13; no macOS, no 3.11 | CI | LOW |
| `dependabot-config` — no `.github/dependabot.yml` for pip + Actions ecosystems | Dependency | LOW |
| `session-resume-chmod` — `CLAUDE_ENV_FILE` written without `0o600` chmod | Security | LOW |
| `brace-guard` — `}` not blocked by `_DANGEROUS_COMPOUND_RE` in confirmation wrapper | Security | LOW |
| `intent-sdlc-dead-code` — `if __name__ == "__main__": pass` no-op artifact at lines 335–336 | Code Health | LOW |
| `adr-summary-tests` — pipe-escaping + truncation boundary cases untested in `adr_summary.py` | Test | LOW |
| `readme-dir-fix` — doubled path component `project/project/context.md` in README dir structure | Docs | LOW |
| `weak-test-assertions` — ~335 `assert "keyword" in content` checks on .md files; no behavioral signal | Test | LOW |
| `hook-timing-log` — no structured hook execution timing; no way to ID latency contributors | Observability | LOW |
| `pytest-markers-consolidate` — `error_path` marker in `conftest.py` not `pytest.ini` | DX | LOW |
| `safety-agent-length-cap` — no command length cap before subagent prompt in `safety_check_agent` | Security | LOW |
| `nosec-annotation` — `nosec B310` in `call_zie_memory_api` suppresses entire check class | Security | LOW |
| `commitizen-pin` / `markdownlint-pin` — version unpinned in `.cz.toml` / `npx` invocation | Dependency | LOW |
| `hook-timing-log` — no structured hook execution timing | Observability | LOW |

---

## Dimensions with 0 findings

None — all 7 dimensions produced findings.

---

## Sources

- CVE-2025-71176: <https://nvd.nist.gov/vuln/detail/CVE-2025-71176>
- CVE-2025-59288: <https://github.com/advisories/GHSA-7mvr-c777-76hp>
- Python Developer Tooling Handbook: <https://pydevtools.com/handbook/>
- Modern Python Project Setup 2025: <https://albertsikkema.com/python/development/best-practices/2025/10/31/modern-python-project-setup.html>
- pytest testing guide: <https://realpython.com/pytest-python-testing/>
